model_list:
  - model_name: default
    litellm_params:
      model: os.environ/LITELLM_MODEL
      api_key: os.environ/LITELLM_API_KEY

general_settings:
  drop_params: true  # ignore params unsupported by the provider (e.g. top_k)
